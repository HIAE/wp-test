"use strict";

// Inicia namespace
var app = {};

// jQuery (carregado à parte porque existem plugins dependentes do jQuery)
//=require _jquery.js

// Plugins (sem ser minified)
//=require plugins/*.js

// Módulos
//=require modules/*.js

// Funções que correm quando em dom ready
//=require _init.js